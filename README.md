Explication du contenu de chaque fichier:

1) main_recursion.cpp:
  Contiens le code main pour executer l'algorithme de solution recursive du probléme.

3) main_iterative.cpp:
  Contiens le code main pour executer l'algorithme de solution iterative du probléme.

4) hanoi_recursion.cpp:
   Contiens la fonction hanoi recursive seule
   
6) hanoiUtils.cpp:
  Contiens quelques fonctions utilisés, partagés entre les deux algorithmes ainsi que la structure de donné utilisé pour simuler les tours de hanoi.
